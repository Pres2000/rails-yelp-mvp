<div class="container">
  <h1>Restaurants</h1>
  <ul>
    <% @restaurants.each do |restaurant| %>
      <li><%= restaurant.name %>
      <%= link_to "details", show_path(restaurant)%>
      </li>
    <% end %>
  </ul>
</div>
